{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && tsx prisma/seed.ts && npx puppeteer browsers install chrome && next build",
  "functions": {
    "src/app/api/**/*.ts": {
      "runtime": "nodejs20.x",
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
